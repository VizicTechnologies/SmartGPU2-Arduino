Place the .VID files under this folder, use the VizicTechnologies .vid converter utility to convert any video format to .vid.
In order to have audio, the .vid file must have the same name as the .wav file, example of contents that must go here:

//A video with sound
  myVideoSound.vid
  myVideoSound.wav

//A video without sound
  myVideoNoSound.vid